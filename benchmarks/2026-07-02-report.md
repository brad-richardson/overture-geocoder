# Before/after benchmark — 2026-07-02 ranking + pipeline overhaul

Date: 2026-07-02
Live version measured: `2026-07-02.1` (5.1 MB HEAD, 0.65 wiki bar, alt-name
ladder, comma truncation, NormalizedQuery hoist, in-SQL legacy boost).
Baseline: `benchmarks/2026-06-11-vs-nominatim-photon.json` (June 11 data +
pre-overhaul ranking).

Raw data: `2026-07-02-vs-nominatim-photon.json` (full-query, 50 queries),
`2026-07-02-typeahead.json` (progressive-prefix, 16 cases).

## Headline

- **Correct-location rate up, wrong-city-with-right-name eliminated** on the
  15-city global set: within-50km went 10/15 → 12/15. June returned Rome GA
  for "Rome" (8,107 km off), a US "Cairo" (2,569 km), a US "Moscow"
  (8,375 km); all three now return the real city (≤1.5 km).
- **Median full-query latency 77 ms → 61 ms** (same 15 queries; 60 ms median
  over the full 50-query US+global set, p95 74 ms). NormalizedQuery hoist +
  always-in-SQL legacy boost.
- **Type-ahead: median 3 keystrokes to top-1 vs Photon's 4**, at 56 ms p50 vs
  Photon's 473 ms — competitive accuracy at ~8x lower latency.

## Full-query vs Nominatim/Photon (15 global cities)

|                        | Overture June | Overture July | Photon | Nominatim (July) |
|------------------------|--------------|---------------|--------|------------------|
| Top-1 name match       | 12/15        | 9/15 †        | 11/15  | 10/15            |
| Within 50 km           | 10/15        | **12/15**     | 12/15  | 11/15            |
| Median latency         | 77 ms        | **61 ms**     | 477 ms | 49 ms            |

Across the full 50-query set (rerun, all engines clean): Overture 78%
within-tolerance / 57 ms avg, Nominatim 74% / 54 ms, Photon 76% / 486 ms.
Overture's average miss distance is 38.8 km vs ~342 km for both others —
when it errs, it errs locally rather than picking a wrong continent.

† The name-match drop is a **metric artifact, not a regression**: the correct
results now surface with local-script primary names (Roma, Москва́, القاهرة)
which fail the benchmark's English string compare, while June's "matches"
were wrong-continent places that happened to share the English name. The
within-50km row is the honest signal. Fixing the artifact means either
localized display names in the API or coordinate-first scoring in the
benchmark.

(The first July attempt got 429-rate-limited by nominatim.openstreetmap.org
because both benchmark scripts ran against it concurrently; the table uses
the clean sequential rerun, `2026-07-02-vs-nominatim-photon-rerun.json`.
Never run both scripts at once — the 1 req/s policy is per IP.)

## Type-ahead (progressive prefixes, top-1 within 50 km of target)

Summary (16 cases, min prefix 3, top-5 window):

|                              | Overture | Photon | Nominatim ‡ |
|------------------------------|----------|--------|-------------|
| Full-query top-1             | 12/16    | 13/16  | 1/16        |
| Resolved before full query   | 10/15    | 12/15  | 1/15        |
| Median keystrokes to top-1   | **3**    | 4      | —           |
| Latency p50                  | **56 ms**| 473 ms | 1,428 ms    |

‡ Nominatim's row is invalid for the same 429 reason; it is also not built
for per-keystroke autocomplete (no prefix search, ~1 s responses, 1 req/s
policy) — Photon is the real type-ahead competitor.

Keystrokes to top-1 per query (— = never reached top-1, see caveats):

| query          | Overture | Photon |
|----------------|----------|--------|
| london         | 4        | 4      |
| paris          | 4        | 4      |
| tokyo          | — †      | — †    |
| berlin         | 3        | 3      |
| sao paulo      | 5        | 5      |
| beijing        | — †      | — †    |
| mexico city    | — (bug)  | 3      |
| amsterdam      | 3        | 3      |
| new york       | 3        | 3      |
| gettysburg     | **3**    | 4      |
| moscow         | **3**    | 4      |
| france         | 6        | 5      |
| germany        | — †      | — †    |
| york           | 3        | 3      |
| san francisco  | 5        | 5      |
| boston, ma     | 10       | 10     |

† Same exonym artifact: Overture returns 東京都 / 北京市 / Deutschland with
correct coordinates; the scorer's name check fails. Photon fails the same
cases for the same reason. Not a ranking defect.

The June regression cases the overhaul targeted all now resolve at 3
keystrokes: **gettysburg** (famous-small, beat Photon), **moscow** (exonym
alt-name rung, beat Photon), **york** (no NYC pollution), **boston, ma**
(comma truncation — top-1 the moment the comma segment completes).

## Real defects found by this benchmark

1. **"mexico city" → Albuquerque, NM at rank 1** (CDMX at rank 2). Root
   cause: `build_shards.py` flattens all name variants into an unordered,
   deduplicated token bag (`search_name` for CDMX is
   `"mx. federal cd. d.f. distrito city mexico cdmx district méxico de
   ciudad"`), so the phrase "mexico city" can never hit the exact/prefix
   rungs of the match ladder — it degrades to partial quality, and a
   same-country result with incidental token hits ("New Mexico" context +
   proximity) wins. Fix direction: store alt names as distinct strings (or
   at least preserve adjacency for common exonyms) so `alt_name_quality`
   can phrase-match. Not attempted today — it is a shard schema change.
2. **Overture source data contains duplicate locality entities**: a full
   scan of the 2026-06-17.0 divisions theme found ~24 same-name locality
   pairs within 15 km of each other in the same country+region among
   pop ≥ 50k places (Kissimmee FL appears 4×; Randburg, Hoover, Villa
   Mercedes and others are exact-coordinate twins). Details and dedup
   strategy: `docs/overture-data-feedback-2026-07-02.md`. Wastes result
   slots; a build-time (name, region, distance) dedup would mask it until
   fixed upstream.

## HEAD shard, post-overhaul

11,004 records / 5.1 MB (219 countries, 3,917 regions, 6,868 localities with
wiki importance ≥ 0.65). Down from 28.9 MB on `.0` (bad single 0.5 bar) and
8.4 MB in June — the 0.65 bar is stricter than June's criteria while keeping
famous-small places searchable globally (Gettysburg, Reykjavík, Zermatt-class
entries live in HEAD or resolve through country shards).

## How to reproduce

```bash
python scripts/benchmark_geocoders.py --output benchmarks/<date>-vs-nominatim-photon.json
python scripts/benchmark_typeahead.py --output benchmarks/<date>-typeahead.json
# Never run both at once: nominatim.openstreetmap.org enforces 1 req/s per IP.
```
